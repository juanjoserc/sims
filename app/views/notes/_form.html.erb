<%= form_for([@ticket, @ticket.notes.build]) do |f| %>
<div class="form-group">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
    <%= f.hidden_field  :user_id, value: @current_user.id %>
    <%= f.submit "Save", class: 'btn btn-success'%>
</div>
<% end %>